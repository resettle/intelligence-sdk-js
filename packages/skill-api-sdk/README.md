# @resettle/skill-api-sdk

A TypeScript SDK client for the Resettle Intelligence skill API.

## Usage

You can install the SDK package from npm

```bash
npm install @resettle/skill-api-sdk
```

If you are using yarn as the package manager, then

```bash
yarn add @resettle/skill-api-sdk
```

If you are using pnpm as the package manager, then

```bash
pnpm add @resettle/skill-api-sdk
```

Next, you can visit [Resettle Developer](https://resettle.ai/dev) to get a free API key to test it out. You can also checkout the [API documentation](https://resettle.github.io/doc#skill)
